version: '3.8'

services:
  tracker:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./db:/app/db
      - ./captures:/app/captures
      - ./logs:/app/logs
    restart: unless-stopped
